# deps
node_modules/

# local env
*.local

# aider
.aider*

# eslint cache
.eslintcache

# build output — dist/ is checked in so `bunx github:HXYerror/copilot-api`
# works without re-running prepare on the user's machine. Source-maps and
# the legacy crew working dir we still keep out.
dist/**/*.map
dist/.crew/
ui/node_modules/
ui/bun.lock

# editor / OS
.DS_Store
*.swp

# Claude Code / harness state
.claude/
.crew/
.harness/

# Python cache
__pycache__/
*.pyc

# Local debug/scratch files
abc.txt
eval-engineer*.md
move-eval.sh
probe*.md
probe*.sh
test-write.md
